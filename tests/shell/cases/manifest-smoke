#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../../.." && pwd)

test -f "$root/manifest.json"
for entry in BarWidget.qml Panel.qml Overlay.qml Service.qml SuperGuide.qml; do
  test -f "$root/$entry"
done

grep -q '"io.github.cyprusad.omakeez"' "$root/manifest.json"
